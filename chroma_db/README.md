# chroma_db

**ChromaDB** 벡터 데이터베이스가 저장되는 디렉터리입니다.

## 역할

- `jsonToLlm` 모듈의 RAG(검색 증강 생성)용 지식 베이스
- HTP 논문/지표에서 추출한 텍스트가 임베딩되어 저장됨
- 챗봇·해석 시 유사 문서 검색에 사용

## 참고

- 실제 DB 경로는 `jsonToLlm`에서 `htp_knowledge_base` 등으로 설정될 수 있음
- 이 폴더는 기본 저장 위치이며, Docker 볼륨으로 영속화할 때 마운트 대상으로 사용할 수 있음
