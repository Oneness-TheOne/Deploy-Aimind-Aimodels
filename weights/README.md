# weights

**모델 가중치** 파일을 두는 디렉터리입니다.

## 용도

- YOLO 등 학습된 모델(.pt 등) 저장
- `image_to_json` 모듈은 주로 `image_to_json/*_weights/` 아래 객체별 가중치를 사용하며, 공통/대용량 가중치를 이 폴더에 둘 수 있음

## 참고

- 대용량 파일은 Git에 올리지 않고, 빌드 시 다운로드하거나 볼륨으로 마운트하는 방식을 권장
- Dockerfile에서 `weights` 디렉터리를 생성만 하고, 실제 파일은 별도 배포 절차로 채우는 구성이 일반적
